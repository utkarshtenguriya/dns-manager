{ 
    "version": 2, 
    "rewrites": [{ "source": "/(.*)", "destination": "/" }],
    "builds": [{
        "src": "/(.*)",
        "use": "@vercel/node"
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
    
}
