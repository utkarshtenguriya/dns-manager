{ 
    "version": 2, 
    "rewrites": [{ "source": "/(.*)", "destination": "/" }],
    "builds": [{
        "src": "./dist",
        "use": "yarn build"
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
    
}
